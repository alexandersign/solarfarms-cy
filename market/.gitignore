# Keep Excel source files local only (too large for git)
excel/*.xlsx
excel/*.xls

# Processed JSON is committed so it deploys to production
# (regenerate with: npx ts-node scripts/download-market-data.ts)

# Keep the folder structure
!.gitignore
!excel/.gitkeep
!data/.gitkeep
